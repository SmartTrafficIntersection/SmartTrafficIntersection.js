/**
 * Grid is made of multiple cells
 */
class Grid {
  /**
   * builds a new grid
   * @param {number} rows
   * @param {number} columns
   */
  constructor(rows, columns) {
    this.rows = rows;
    this.columns = columns;
  }
}

module.exports = Grid;
